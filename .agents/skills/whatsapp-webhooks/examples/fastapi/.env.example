# Meta app secret — signs POST payloads (App Dashboard > App Settings > Basic > App Secret)
WHATSAPP_APP_SECRET=your_meta_app_secret_here

# Verify token — a random string you choose; must match the value in the WhatsApp dashboard
WHATSAPP_VERIFY_TOKEN=your_verify_token_here
